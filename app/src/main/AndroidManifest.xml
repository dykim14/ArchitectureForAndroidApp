<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.practice.architectureforandroidapp">

    <application
        android:allowBackup="true"
        android:name=".chapter2.sub14.App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ArchitectureForAndroidApp">

        <activity android:name=".chapter2.MyActivity">
        </activity>

        <activity android:name=".chapter2.sub14.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".chapter4.sub1.MainActivity" />
        <activity android:name=".chapter4.sub1.BindingMethodActivity" />
        <activity android:name=".chapter4.sub1.ClickBindingActivity" />
    </application>

</manifest>