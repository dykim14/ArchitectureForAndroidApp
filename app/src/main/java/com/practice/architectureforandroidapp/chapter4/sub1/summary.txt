Data binding 라이브러리

layout의 각 요소들을 개발자가 일일이 찾지않고 바로 사용할 수 있도록 해주는 라이브러리

<layout> 태크를 원하는 레이아웃의 최상단에 붙여주면, 해당 레이아웃의 이름 + "Binding"으로 네이밍된 클래스가 만들어진다.
해당 클래스를 사용하여 각 뷰의 id를 직접 호출할 수 있다.

또한 레이아웃 내에서 변수 및 클래스를 불러온 후 해당 값들을 레이아웃 내 뷰에 적용할 수 있다.

Observable 클래스는 한번 바인딩하면 추후 값이 갱신되었을때 자동으로 반영이 된다.